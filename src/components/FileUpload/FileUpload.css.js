import { makeStyles } from '@material-ui/core'

export default makeStyles(theme => ({
    formControl: {
        margin: theme.spacing(1),
    },
    uploadBox: {
        height: '100%',
    },
    select: {
        width: '50%',
        minWidth: '200px',
        marginBottom: '15px',
    },
}))
